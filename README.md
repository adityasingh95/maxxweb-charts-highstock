# MaxxWeb Charts Highstock

Initial placeholder so the repository has a base `main` commit. The Highstock demo implementation is being prepared on a separate feature branch and will be proposed through a pull request.
